.DS_Store

# What GitHub Pages serves lives in docs/ (Pages source = main /docs).
# Everything outside docs/ (CLAUDE.md, content/schemas + prompt + report) is
# version-controlled but NOT public — the docs/ boundary is what keeps it off Pages.
